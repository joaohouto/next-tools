import { Converter } from "./client";

export const metadata = {
  title: "Conversor",
  description: "Converta qualquer tipo de arquivo.",
};

export default function ConverterPage() {
  return <Converter />;
}
